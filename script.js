let arr = [1, 2, 3, 4, 5];
arr.forEach((val) => {
  console.log(val);
});
//console.log?(val.toUpperCase());// value alphabet
// for a given array of numbers,print the square of each value using the forEach loop.
let nums = [2, 3, 4, 5, 6];
nums.forEach((num) => {
  console.log(num * num); //num**2
});
//filter
//creates a new array
